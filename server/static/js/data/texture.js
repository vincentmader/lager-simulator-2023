export class Texture {

    constructor(img_path, img_dimensions, texture_origin, texture_scale) {
        this.img_path = img_path;
        this.img_dimensions = img_dimensions;
        this.texture_origin = texture_origin;
        this.texture_scale = texture_scale;
    }
}
